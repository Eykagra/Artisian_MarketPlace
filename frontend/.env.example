# Copy this to .env.local for local dev, or set as Vercel environment variable in production.
# Must start with VITE_ to be visible in the browser bundle.

# Your Render backend URL (no trailing slash)
VITE_API_URL=https://your-backend.onrender.com
